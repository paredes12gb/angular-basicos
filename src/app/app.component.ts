import { Component } from '@angular/core';
import { Validators } from '@angular/forms';

@Component({ //decorador de nombre Componente
  selector: 'app-root', //ubicado en el index.html este es el nombre del componente
  templateUrl: 'app.component.html', //url del template a utilizar, siempre esta ubicado en el misma carpeta
  styleUrls: ['app.component.css']
})
export class AppComponent {
 
}
